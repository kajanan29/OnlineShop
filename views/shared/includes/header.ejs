 <header>
    <div>
        <a href="/">WDE</a>
    </div>
    <nav>
        <ul>
            <%if(isAuth && !locals.hideLogoutButton) {%>
            <li>
                <form action="/logout" method="post">
                    <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                  <button >Logout</button>
                </form>
                
            </li>
            <% }  %>
        </ul>
    </nav>
 </header>